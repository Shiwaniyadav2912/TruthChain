# TruthChain
**TruthChain** is a browser-based web app that verifies whether a news article or image has been altered using **SHA-256 hashing**. It compares digital fingerprints, flags suspicious writing patterns, includes a ledger explorer with demo cases, and runs entirely in the browser with no backend.
